interface:
  display_name: "Spacefast"
  short_description: "Publish files and folders to Spacefast."
  brand_color: "#FF603D"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "spacefast"
      description: "Spacefast MCP server for managing your spaces, domains and more."
      transport: "streamable_http"
      url: "https://mcp.spacefast.com"
